package com.fooda.wadalzaki.utils;

public class Convertors {
    public Convertors() {
    }
//    public String convertToPinYin(String name){
////       return Trans2PinYin.getInstance().convertAll(name).toUpperCase();
////    }
//    public UserInfo fromRemoteToLocal(RemoteUser user){
//        return new UserInfo(user.getAddress(), user.getEmail(), user.getImagePath(), user.getName(), user.getNumberThree(),user.getNumberTwo(),user.getPhoneNumber(), user.getUid());
//    }
//
}
